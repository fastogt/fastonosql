CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(unqlite)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_LIST_DIR}/src)

SET(HEADERS_SSDB
    src/unqlite.h
)

SET(SOURCES_SSDB
    src/unqlite.c
)

ADD_LIBRARY(unqlite OBJECT ${HEADERS_SSDB} ${SOURCES_SSDB})

